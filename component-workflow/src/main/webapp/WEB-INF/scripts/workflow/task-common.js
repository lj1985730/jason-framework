/**
 *  任务-通用脚本
 */
!function ($) {

    'use strict';

    var baseUrl = appPath + '/workflow';

    var $table;

    /**
     *  初始化
     */
    $(function () {

        /**
         * 查看流程
         * @param processInstanceId 流程实例ID
         */
        $.fn.viewWorkflowProcess = function (processInstanceId) {

        };
    });

}(window.jQuery);